import React from "react"
import Layout from "../components/Layout"

    const places = () => {

            return (
            <Layout>
                <h1>places page</h1>
            </Layout>
        )
    }

    export default places